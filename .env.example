# Example environment variables (do NOT commit real secrets)

# OpenAI API key for assistant
OPENAI_API_KEY=

# MongoDB connection (recommended)
MONGO_URI=mongodb://localhost:27017
MONGO_DB_NAME=amazon_clone_db

# Optional: override default FastAPI host/port
FASTAPI_HOST=127.0.0.1
FASTAPI_PORT=8000
